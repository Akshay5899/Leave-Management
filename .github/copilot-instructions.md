# Project instructions

- Use React with Vite for the client and Express/Mongoose for the API.
- Keep leave status transitions atomic and duplicate-safe.
- Use MongoDB Atlas through `MONGODB_URI`; demo memory mode is allowed for local UI work.
- Validate changes with `npm run build` and focused API checks when possible.
